<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.sm.app.repository.MapCourseRepository">

    <!-- 기본 CRUD 쿼리 -->
    
    <!-- 전체 조회 -->
    <select id="selectAll" resultType="MapCourse">
        SELECT *
        FROM Map_Course
        WHERE is_deleted = 'N'
        ORDER BY course_regdate DESC
    </select>

    <!-- 단건 조회 (course_id로) -->
    <select id="select" parameterType="Integer" resultType="MapCourse">
        SELECT *
        FROM Map_Course
        WHERE course_id = #{courseId} AND is_deleted = 'N'
    </select>

    <!-- rec_id로 코스 목록 조회 -->
    <select id="selectByRecId" parameterType="Integer" resultType="MapCourse">
        SELECT *
        FROM Map_Course
        WHERE rec_id = #{recId} AND is_deleted = 'N'
        ORDER BY course_regdate DESC
    </select>

    <!-- 코스 타입별 조회 -->
    <select id="selectByCourseType" parameterType="String" resultType="MapCourse">
        SELECT *
        FROM Map_Course
        WHERE course_type = #{courseType} AND is_deleted = 'N'
        ORDER BY course_regdate DESC
    </select>

    <!-- 삽입 (등록) -->
    <insert id="insert" parameterType="MapCourse" useGeneratedKeys="true" keyProperty="courseId">
        INSERT INTO Map_Course (
            rec_id,
            course_name,
            course_type,
            course_path_data,
            is_deleted,
            course_regdate,
            course_update
        )
        VALUES (
            #{recId},
            #{courseName},
            #{courseType},
            #{coursePathData},
            'N',
            NOW(),
            NOW()
        )
    </insert>

    <!-- 수정 -->
    <update id="update" parameterType="MapCourse">
        UPDATE Map_Course SET
        <if test="recId != null">
            rec_id = #{recId},
        </if>
        <if test="courseName != null and !courseName.equals('')">
            course_name = #{courseName},
        </if>
        <if test="courseType != null and !courseType.equals('')">
            course_type = #{courseType},
        </if>
        <if test="coursePathData != null and !coursePathData.equals('')">
            course_path_data = #{coursePathData},
        </if>
        <if test="isDeleted != null and !isDeleted.equals('')">
            is_deleted = #{isDeleted},
        </if>
        course_update = NOW()
        WHERE course_id = #{courseId}
    </update>

    <!-- 삭제 (논리 삭제) -->
    <delete id="delete" parameterType="Integer">
        UPDATE Map_Course 
        SET is_deleted = 'Y', course_update = NOW() 
        WHERE course_id = #{courseId}
    </delete>

</mapper>

